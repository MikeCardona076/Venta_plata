#include <stdio.h>
#include <string.h>
int main(){
    printf("Hola mike");
    return 0;
}
// para compilar un archivo y guardarlo con la extension que quieras
// gcc 'nombre de archivo c' -o 'nombre de archivo para compilar'.out
// Esto es en Ubuntu en Windows se necesita otra nomenclatura

// Abrir archivo ./'nombre de archivo'.out